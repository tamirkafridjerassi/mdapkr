version: '3'
services:
  fastapi:
    build: .
    ports:
      - "8000:8000"
    environment:
      MONGO_URL: mongodb://mongo:27017
    depends_on:
      - mongo
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

  mongo:
    image: mongo:6
    ports:
      - "27017:27017"
